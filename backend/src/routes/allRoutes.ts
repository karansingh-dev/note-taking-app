/*
Import all controller files so that javascript recognise all these controllers at run time, 
because we are using custom routing function and 
this file will be imported in the index.ts so that the index file does not look messy and ugly
*/

import "../controller/user.controller.js";
